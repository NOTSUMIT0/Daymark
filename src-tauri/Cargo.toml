[package]
name = "daymark-desktop"
version = "1.0.0"
description = "Daymark Windows Desktop App"
authors = ["Daymark Systems"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0.0", features = [] }

[dependencies]
tauri = { version = "2.0.0", features = [] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
